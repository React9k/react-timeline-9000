export const timelineScenarios = {
  givenRowsThenRender: 'GIVEN we feed some rows (groups) THEN they are rendered',
  givenSegmentsThenRender: 'GIVEN we feed (for each row) some segments (items) THEN they are rendered',
  whenMouseMovesThenRedBar:
    'WHEN the mouse moves THEN the red bar moves also (with snapping) AND the its date is displayed'
};
